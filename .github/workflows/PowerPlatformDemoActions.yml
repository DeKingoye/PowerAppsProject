name: Power Platform Export and Publish

on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: 🧩 Checkout repository
        uses: actions/checkout@v4

      # ✅ Étape pour rendre NuGet accessible
      - name: 🧰 Ensure NuGet is available
        run: |
          Write-Host "Manually setting nuget.exe path..."
          $nugetPath = "C:\Windows\System32\nuget.exe"
          if (-Not (Test-Path $nugetPath)) {
            Write-Host "nuget.exe not found, downloading..."
            Invoke-WebRequest "https://dist.nuget.org/win-x86-commandline/latest/nuget.exe" -OutFile $nugetPath
          }
          Write-Host "Using nuget at: $nugetPath"
          & $nugetPath help
        shell: pwsh

      # ⚙️ Installer Power Platform Tools avec le bon nuget.exe
      - name: ⚙️ Install Power Platform Tools
        uses: microsoft/powerplatform-actions/actions-install@v1
        env:
          NUGET_EXE_PATH: C:\Windows\System32\nuget.exe

      # 🔧 Forcer le chemin PAC CLI (dotnet global tools)
      - name: 🔧 Force PAC path for self-hosted runner
        run: |
          Write-Host "Setting PAC_PATH to .dotnet global tools path..."
          echo "PAC_PATH=$env:USERPROFILE\.dotnet\tools\pac.exe" >> $env:GITHUB_ENV
        shell: pwsh

      # 🔍 Vérification du CLI
      - name: 🔍 Verify PAC installation
        run: |
          Write-Host "PAC path: $env:PAC_PATH"
          & $env:PAC_PATH help
        shell: pwsh

      # 📦 Exporter la solution
      - name: 📦 Export Solution
        uses: microsoft/powerplatform-actions/export-solution@v1
        with:
          environment-url: 'https://org643d7b2c.crm12.dynamics.com'
          user-name: 'nicoise.nkounkou@edu.igensia.com'
          password-secret: ${{ secrets.PASSWORD }}
          solution-name: 'aSolution'
          solution-output-file: 'aSolution.zip'
          working-directory: 'out'
        env:
          POWERPLATFORMTOOLS_PACINSTALLED: true
          POWERPLATFORMTOOLS_PACPATH: ${{ env.PAC_PATH }}

      # 📂 Décompresser la solution
      - name: 📂 Unpack Solution
        uses: microsoft/powerplatform-actions/unpack-solution@v1
        with:
          solution-file: 'out/aSolution.zip'
          solution-folder: 'out/solutions/solution-one'
          solution-type: 'Unmanaged'
          overwrite-files: true
        env:
          POWERPLATFORMTOOLS_PACPATH: ${{ env.PAC_PATH }}

      # 🚀 Publier la solution
      - name: 🚀 Publish Solution
        uses: microsoft/powerplatform-actions/publish-solution@v1
        with:
          environment-url: 'https://org643d7b2c.crm12.dynamics.com'
          user-name: 'nicoise.nkounkou@edu.igensia.com'
          password-secret: ${{ secrets.PASSWORD }}
        env:
          POWERPLATFORMTOOLS_PACPATH: ${{ env.PAC_PATH }}

      # 🧾 Commit éventuel
      - name: 🧾 Commit solution changes
        uses: microsoft/powerplatform-actions/branch-solution@v1
        with:
          solution-folder: 'out/solutions/solution-one'
          solution-target-folder: 'src/solutions/solution1'
          token: ${{ secrets.GIT_TOKEN }}

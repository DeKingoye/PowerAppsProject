name: Power Platform Export and Publish

on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: 🧩 Checkout repository
        uses: actions/checkout@v4

      # --- Étape de vérification du CLI Power Platform ---
      - name: 🔍 Verify PAC installation
        run: |
          if (-Not (Test-Path "C:\tools\pac\pac.exe")) {
            Write-Error "❌ pac.exe introuvable dans C:\tools\pac — vérifie l'installation."
            exit 1
          }
          Write-Host "✅ PAC CLI détecté dans C:\tools\pac"
          & "C:\tools\pac\pac.exe" --help
        shell: pwsh

      # --- Étape d'export de la solution ---
      - name: 📦 Export Solution
        uses: microsoft/powerplatform-actions/export-solution@v1
        with:
          environment-url: 'https://org643d7b2c.crm12.dynamics.com'
          user-name: 'nicoise.nkounkou@edu.igensia.com'
          password-secret: ${{ secrets.PASSWORD }}
          solution-name: 'aSolution'
          solution-output-file: 'aSolution.zip'
          working-directory: 'out'
          managed: false
          run-asynchronously: false
        env:
          POWERPLATFORMTOOLS_PACPATH: C:\tools\pac\pac.exe
          PAC_PATH: C:\tools\pac\pac.exe
          POWERPLATFORMTOOLS_PACINSTALLED: true

      # --- Étape de décompression de la solution ---
      - name: 📂 Unpack Solution
        uses: microsoft/powerplatform-actions/unpack-solution@v1
        with:
          solution-file: 'out/aSolution.zip'
          solution-folder: 'out/solutions/solution-one'
          solution-type: 'Unmanaged'
          overwrite-files: true
        env:
          POWERPLATFORMTOOLS_PACPATH: C:\tools\pac\pac.exe

      # --- Étape de publication de la solution ---
      - name: 🚀 Publish Solution
        uses: microsoft/powerplatform-actions/publish-solution@v1
        with:
          environment-url: 'https://org643d7b2c.crm12.dynamics.com'
          user-name: 'nicoise.nkounkou@edu.igensia.com'
          password-secret: ${{ secrets.PASSWORD }}
        env:
          POWERPLATFORMTOOLS_PACPATH: C:\tools\pac\pac.exe

      # --- Étape finale : commit des modifications ---
      - name: 🧾 Commit solution changes
        uses: microsoft/powerplatform-actions/branch-solution@v1
        with:
          solution-folder: 'out/solutions/solution-one'
          solution-target-folder: 'src/solutions/solution1'
          token: ${{ secrets.GIT_TOKEN }}
